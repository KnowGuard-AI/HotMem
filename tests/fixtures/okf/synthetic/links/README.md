# Synthetic OKF edge-case fixtures (#68)

Hand-written (not upstream): v0.1 legacy fields (timestamp fallback,
spec §13.1), malformed frontmatter shapes, and a deep hierarchy with
absolute/relative/external/broken links and no index.md (spec §11
tolerance). Consumed by tests/test_okf_conformance.py.
